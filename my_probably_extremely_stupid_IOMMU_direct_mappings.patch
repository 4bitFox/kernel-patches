diff -rupN linux-6.14.7/drivers/iommu/iommu.c linux-6.14.7_patched/drivers/iommu/iommu.c
--- linux-6.14.7/drivers/iommu/iommu.c	2025-05-18 08:26:10.000000000 +0200
+++ linux-6.14.7_patched/drivers/iommu/iommu.c	2025-05-19 20:50:11.783776347 +0200
@@ -2234,8 +2234,8 @@ static int __iommu_device_set_domain(str
 	    (new_domain->type == IOMMU_DOMAIN_BLOCKED ||
 	     new_domain == group->blocking_domain)) {
 		dev_warn(dev,
-			 "Firmware has requested this device have a 1:1 IOMMU mapping, rejecting configuring the device without a 1:1 mapping. Contact your platform vendor.\n");
-		return -EINVAL;
+			 "Firmware has requested this device have a 1:1 IOMMU mapping. I dont care tough! **Patches out probably very important safety check :-D**\n");
+		// return -EINVAL;
 	}
 
 	if (dev->iommu->attach_deferred) {
